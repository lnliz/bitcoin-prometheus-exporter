name: Docker

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build --build-arg COMMIT=${{ github.sha }} -f Dockerfile -t lnliz/bitcoin-prometheus-exporter:${{ github.sha }} .

      - name: Login to Docker Hub
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: echo "${{ secrets.DOCKER_PASS }}" | docker login -u "${{ secrets.DOCKER_USER }}" --password-stdin

      - name: Tag and push latest
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          docker tag lnliz/bitcoin-prometheus-exporter:${{ github.sha }} lnliz/bitcoin-prometheus-exporter:latest
          docker push lnliz/bitcoin-prometheus-exporter:latest
